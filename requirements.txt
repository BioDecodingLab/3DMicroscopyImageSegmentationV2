# Dependencies are declared in pyproject.toml (single source of truth).
# This installs the project in editable mode together with all its dependencies,
# so it is equivalent to running `pip install -e .`.
-e .
